export type Entity = {
  name: string;
  id: number;
};

export type DrinkType = {
  id: string;
  name: string;
  description: string;
  instructions: string;
};

export type EquipmentType = {
  id: string;
  name: string;
};

export type IngredientType = {
  id: string;
  name: string;
};
